
export const NotFound = ({ title }: { title: string }) => {
    return (
        <div className="dark flex items-center justify-center h-[70vh] text-white scrollbar-hide">
            {title}
        </div>
    )
}
